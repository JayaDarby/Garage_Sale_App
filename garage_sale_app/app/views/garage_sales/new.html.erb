  <body ng-controller="GarageSaleController" nv-file-drop="" uploader="uploader" filters="queueLimit, customFilter">

        <!-- Fix for old browsers -->
        <script src="http://nervgh.github.io/js/es5-shim.min.js"></script>
        <script src="http://nervgh.github.io/js/es5-sham.min.js"></script>
        <script src="http://code.jquery.com/jquery-1.8.3.min.js"></script>
        <!--- <script src="../console-sham.js"></script> --->

        <script src="http://netdna.bootstrapcdn.com/bootstrap/3.0.0/js/bootstrap.min.js"></script>

        <!--<script src="../bower_components/angular/angular.js"></script>-->
        

        <style>
            .my-drop-zone { border: dotted 3px lightgray; }
            .nv-file-over { border: dotted 3px red; } /* Default class applied to drop zones on over */
            .another-file-over-class { border: dotted 3px green; }
            html, body { height: 100%; }
            #leftNewGarageSale{float:left;
                                width:730px;}
            #rightNewGarageSale{float:left;
                                width:400px;}
            input[type='checkbox']{
                width:10px;
            }

            input{
                width: 400px;
            }
            #garage_sale_zip_code{
                width:200px;
            }
            textarea.form-control {
                width:500px;
                height:250px;
                border-radius: 3px;
            }
            #garage_sale_title{
                width: 500px;
            }

            

        </style>
        <script>
        $(document).ready(function(){
            $('[data-toggle="popover"]').popover(); 
        });
        </script>
        <div class='container'>
        <h4>New Garage Sale</h4>
        <%= form_for [@user, @garage_sale], html: { multipart: true } do |f| %>
        <div id='leftNewGarageSale'>
            <%= f.label "Posting Title"%><br />
            <%= f.text_field :title, :class => 'form-control' %><br />
            <%= f.label "Description" %><br />
            <%= f.text_area :description, :class => 'form-control' %><br />
            <a tabindex="0" id='example' class="btn" role="button" data-toggle="popover" data-trigger="focus" data-content="Your description can include items for sale, prices, driving directions, or parking restrictions"><%= image_tag('questionMark.png')%>  What should My Description Have? </a>
        </div>
        <div id='rightNewGarageSale'>
            <%= f.label "Address" %><br />
            <%= f.text_field :address, :class => 'form-control' %><br />
            <%= f.label "City" %><br />
            <%= f.text_field :city, :class => 'form-control' %><br />
            <div style='width:400px'>
                <div style='float:left'>
            <%= f.label "State" %><br />
            <%= f.select :state, options_for_select(us_states), :class => 'form-control'%><br />
                </div>
                <div style='float:right'>
            <%= f.label "Zip Code" %><br />
            <%= f.text_field :zip_code, :class => 'form-control' %><br />
                </div>
            </div>
            <%= f.label "Phone Number" %><br />
            <%= f.telephone_field :phone_number, :class => 'form-control'%><br />
            <%= f.label "Email" %><br />
            <%= f.email_field :email, :class => 'form-control' %><br />
            <%= f.label "Users Can Contact Me With:" %><br />
            <%= f.check_box :can_email %>
            <%= f.label "Email" %><br />
            <%= f.check_box :can_call %>
            <%= f.label "Call" %><br />
            <%= f.check_box :can_text %>
            <%= f.label "Text" %><br />
            <%= f.submit "New Garage Sale",  { :'ng-click' => 'hello()'}  %>
        </div>
        <% end %>
        </div><br>

        <div class="container">
            <div class="row">
                <div class="col-md-3">
                    <h3>Select Photos</h3>
                    <div ng-show="uploader.isHTML5">
                        <!-- 3. nv-file-over uploader="link" over-class="className" -->
                        <!-- Example: nv-file-drop="" uploader="{Object}" options="{Object}" filters="{String}" -->
                        <div nv-file-drop="" uploader="uploader" options="{ url: '/foo' }">
                            <div nv-file-over="" uploader="uploader" over-class="another-file-over-class" class="well my-drop-zone">
                                Drag Images Here
                            </div>
                        </div>
                    </div>
                    <!-- Example: nv-file-select="" uploader="{Object}" options="{Object}" filters="{String}" -->
                    <input type="file" nv-file-select="" uploader="uploader" multiple  /><br/>
                </div>

                <div class="col-md-9" style="margin-bottom: 40px">

                    <h3>Upload Queue</h3>
                    <p>Queue length: {{ uploader.queue.length }}</p>

                    <table class="table">
                        <thead>
                            <tr>
                                <th width="50%">Name</th>
                                <th ng-show="uploader.isHTML5">Size</th>
                                <th ng-show="uploader.isHTML5">Progress</th>
                                <th>Status</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr ng-repeat="item in uploader.queue">
                                <td><strong>{{ item.file.name }}</strong></td>
                                <td ng-show="uploader.isHTML5" nowrap>{{ item.file.size/1024/1024|number:2 }} MB</td>
                                <td ng-show="uploader.isHTML5">
                                    <div class="progress" style="margin-bottom: 0;">
                                        <div class="progress-bar" role="progressbar" ng-style="{ 'width': item.progress + '%' }"></div>
                                    </div>
                                </td>
                                <td class="text-center">
                                    <span ng-show="item.isSuccess"><i class="glyphicon glyphicon-ok"></i></span>
                                    <span ng-show="item.isCancel"><i class="glyphicon glyphicon-ban-circle"></i></span>
                                    <span ng-show="item.isError"><i class="glyphicon glyphicon-remove"></i></span>
                                </td>
                                <td nowrap>
                                    <button type="button" class="btn btn-success btn-xs" ng-click="item.upload()" ng-disabled="item.isReady || item.isUploading || item.isSuccess">
                                        <span class="glyphicon glyphicon-upload"></span> Upload
                                    </button>
                                    <button type="button" class="btn btn-warning btn-xs" ng-click="item.cancel()" ng-disabled="!item.isUploading">
                                        <span class="glyphicon glyphicon-ban-circle"></span> Cancel
                                    </button>
                                    <button type="button" class="btn btn-danger btn-xs" ng-click="item.remove()">
                                        <span class="glyphicon glyphicon-trash"></span> Remove
                                    </button>
                                </td>
                            </tr>
                        </tbody>
                    </table>

                    <div>
                        <div>
                            Queue progress:
                            <div class="progress" style="">
                                <div class="progress-bar" role="progressbar" ng-style="{ 'width': uploader.progress + '%' }"></div>
                            </div>
                        </div>
                        <button type="button" class="btn btn-success btn-s" ng-click="uploader.uploadAll()" ng-disabled="!uploader.getNotUploadedItems().length">
                            <span class="glyphicon glyphicon-upload"></span> Upload all
                        </button>
                        <button type="button" class="btn btn-warning btn-s" ng-click="uploader.cancelAll()" ng-disabled="!uploader.isUploading">
                            <span class="glyphicon glyphicon-ban-circle"></span> Cancel all
                        </button>
                        <button type="button" class="btn btn-danger btn-s" ng-click="uploader.clearQueue()" ng-disabled="!uploader.queue.length">
                            <span class="glyphicon glyphicon-trash"></span> Remove all
                        </button>
                    </div>

                </div>

            </div>

        </div>


    </body>
</html>





